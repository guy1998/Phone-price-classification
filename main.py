from Algorithms.feed_forward_neural_network import mlp_model
from Algorithms.ensemble_models import ensemble_model
from Algorithms.decision_tree import decision_tree

if __name__ == "__main__":
    # decision_tree("fe_resolution", "min_max")
    ensemble_model("normal", "min_max")
